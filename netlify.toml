[build]
  command   = "npm run build"
  publish   = "dist"

# SPA routing: redirige todo al index.html para que vue-router funcione
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
